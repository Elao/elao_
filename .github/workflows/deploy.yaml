name: Deploy site

on:
  workflow_dispatch: ~

jobs:

  deploy-site:
    name: '🚧  Build & deploy site 🚀'
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: 'Checkout'
        uses: actions/checkout@v2
