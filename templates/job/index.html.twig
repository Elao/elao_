{% extends 'base.html.twig' %}

{% block content %}
    Nous recrutons

    <div>
        {% for job in jobs %}
            <a href="{{ path('job_show', { job: job.slug }) }}">
                <h2>{{ job.title }}</h2>
            </a>
        {% endfor %}
    </div>
{% endblock %}
