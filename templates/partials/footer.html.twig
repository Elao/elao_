{% import "macros.html.twig" as macros %}

<footer class="footer">
    <div class="container">
        <section class="footer__links">
            <p class="h1">
                elao
                <span>développe</span>
                <span>du lien</span>
            </p>
            <div class="links">
                {% for col in site.footer %}
                    <ul>
                        {% for item in col %}
                            <li>
                                <a href="{{ path(item.path) }}" {{ macros.htmlAttributes(item.attributes|default([])) }}>{{ item.label }}</a>
                            </li>
                        {% endfor %}
                    </ul>
                {% endfor %}
                <ul>
                    <li>
                        <a href="{{ path('carriere') }}">Page Carrière</a>
                    </li>
                    <li>
                        <a href="{{ path('glossary') }}">Glossaire 🧐</a>
                    </li>
                    <li>
                        <a href="{{ path('elaomojis') }}">Elaomojis 🎉</a>
                    </li>
                </ul>
            </div>
            <div class="socials">
                <ul>
                    <li>
                        <a href="{{ site.social.linkedin }}" target="_blank">
                            <i class="icon icon--linkedin"></i>
                            Linkedin
                        </a>
                    </li>
                    <li>
                        <a href="{{ site.social.bluesky }}" target="_blank">
                            <i class="icon icon--bluesky"></i>
                            Bluesky
                        </a>
                    </li>
                    <li>
                        <a href="{{ site.social.twitter }}" target="_blank">
                            <i class="icon icon--x"></i>
                            X
                        </a>
                    </li>
                    <li>
                        <a href="{{ site.social.github }}" target="_blank">
                            <i class="icon icon--github"></i>
                            Github
                        </a>
                    </li>
                </ul>
            </div>
            <div class="contacts">
                <ul>
                    <li>
                        <a href="{{ path('contact') }}">Contact</a>
                    </li>
                    <li>
                        <a href="tel:{{ site.contact.phone }}">{{ site.contact.phone }}</a>
                    </li>
                    <li>
                        <a href="mailto:{{ site.contact.email }}">{{ site.contact.email }}</a>
                    </li>
                    <li>{{ site.contact.address.street_address }}</li>
                    <li>{{ site.contact.address.address_postalCode }} {{ site.contact.address.address_locality }}</li>
                </ul>
            </div>
        </section>

        <section class="footer__legals">
            <span>© 2005 - {{ 'now'|date('Y') }} - ELAO - Tous droits réservés</span>
            <a href="{{ path('legal') }}">Mentions légales</a>
            <a href="{{ path('privacy') }}">Protection des données</a>
            <a id="see" href="#ssss">🐍</a>
        </section>
    </div>
</footer>
